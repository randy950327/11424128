# 九、問題背景（機械振動分析）

### ▶ 題目敘述
單缸四衝程柴油機，轉速 n = 1500 r/min，活塞燃氣爆發力的週期 T = 2 * (60/n) = 0.08 s。
一個週期內爆發力隨曲軸轉角 θ = ωt（ω = 2π/T = 25π rad/s）的近似變化為：

* F(θ) = Fm * sin(θ)  (當 0 < θ < π 時)
* F(θ) = 0            (當 π < θ < 2π 時)

其中最大爆發力 Fm = 100 kN。
請將激勵力展開為傅立葉級數，分析振動的頻率成分，並判斷是否會和柴油機機體的固有頻率發生共振（機體一階固有頻率 fn = 25 Hz）。

---

### 1. 系統基本參數與週期分析

* 轉速 n = 1500 r/min
* 激勵力基本週期 T：
  T = 2 * (60 / 1500) = 0.08 s
* 激勵力基頻 (Base Frequency) f0：
  f0 = 1 / T = 1 / 0.08 = 12.5 Hz
* 基本角頻率 ω：
  ω = 2π * f0 = 25π rad/s

---

### 2. 激勵力的傅立葉級數展開

將週期為 2π 的函數 F(θ) 展開為傅立葉級數：
F(θ) = (a0 / 2) + Σ (ak * cos(kθ) + bk * sin(kθ))

利用傅立葉係數公式進行積分（因為在 π 到 2π 區間 F(θ)=0，只需積分 0 到 π）：

* 直流分量 a0：
  a0 = (1/π) * ∫ F(θ) dθ = (2 * Fm) / π

* 一次諧波係數 a1, b1：
  a1 = 0
  b1 = Fm / 2

* 高次諧波係數 ak, bk (當 k ≥ 2)：
  當 k 為奇數時，ak = 0
  當 k 為偶數時，ak = -2Fm / [π * (k² - 1)]
  同理可證，當 k ≥ 2 時，bk = 0。

綜合以上，傅立葉級數展開為：
F(t) = (Fm / π) + (Fm / 2)*sin(ωt) - (2Fm / π) * [ (1/3)cos(2ωt) + (1/15)cos(4ωt) + ... ]

---

### 3. 振動頻率成分分析

由上述展開式可知，激勵力的頻率成分包含：
* 直流恆力（不引起振動）
* 一階諧波（基頻）： f1 = f0 = 12.5 Hz，振幅為 Fm / 2 = 50 kN
* 二階諧波： f2 = 2 * f0 = 25 Hz，振幅為 200 / 3π ≒ 21.22 kN
* 四階諧波： f4 = 4 * f0 = 50 Hz，振幅為 200 / 15π ≒ 4.24 kN
* 以及其他更高階的偶數倍頻率成分。

---

### 4. 共振判斷分析

共振發生條件：當激勵力的某個諧波頻率 fk 與機體固有頻率 fn 相等時，就會引發共振。

已知機體一階固有頻率為：
fn = 25 Hz

將固有頻率與我們算出的頻率成分進行比對：
f2 = 2 * f0 = 2 * 12.5 = 25 Hz

【最終結論】
因為二階諧波的頻率 f2 完全等於柴油機機體的一階固有頻率 fn (25 Hz)，且該二階諧波具備極大的激勵力振幅（約 21.22 kN）。

👉 結論：會發生共振。
在轉速 1500 r/min 運轉下，引擎會因為強烈的二階振動而與機體產生嚴重共振，工程設計上必須加入減振設計或避開此轉速。
