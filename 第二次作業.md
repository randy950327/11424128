# 題目(3)：使用拉普拉斯轉換求解微分方程

### ▶ 問題描述
求解以下二階常微分方程式（ODE）：

$$\frac{d^2x(t)}{dt^2} + 6\frac{dx(t)}{dt} + 8x(t) = 1$$

**已知初始條件：**
* $x(0) = 1$
* $x'(0) = 0$

---

### 1. 對方程式兩邊取拉普拉斯轉換 (Laplace Transform)

根據拉普拉斯轉換的線性性質，我們對方程式的每一項分別取轉換 $\mathcal{L}$：

$$\mathcal{L}\left[ \frac{d^2x}{dt^2} \right] + 6\mathcal{L}\left[ \frac{dx}{dt} \right] + 8\mathcal{L}[x(t)] = \mathcal{L}[1]$$

令 $X(s) = \mathcal{L}[x(t)]$，並代入微分的轉換公式以及常數的轉換公式 $\mathcal{L}[1] = \frac{1}{s}$：

$$[s^2X(s) - sx(0) - x'(0)] + 6[sX(s) - x(0)] + 8X(s) = \frac{1}{s}$$

---

### 2. 代入初始條件

將已知的初始條件 $x(0) = 1$ 與 $x'(0) = 0$ 代入上式：

$$[s^2X(s) - s(1) - 0] + 6[sX(s) - 1] + 8X(s) = \frac{1}{s}$$

展開並重新整理括號：

$$s^2X(s) - s + 6sX(s) - 6 + 8X(s) = \frac{1}{s}$$

---

### 3. 移項並整理出 $X(s)$

將含有 $X(s)$ 的項保留在等式左邊，其餘項移到右邊：

$$X(s)(s^2 + 6s + 8) - s - 6 = \frac{1}{s}$$

$$X(s)(s^2 + 6s + 8) = \frac{1}{s} + s + 6$$

為了方便後續計算，我們將右邊通分合併：

$$X(s)(s^2 + 6s + 8) = \frac{s^2 + 6s + 1}{s}$$

接著，將 $(s^2 + 6s + 8)$ 移至等號右邊的分母，並進行因式分解 $(s^2 + 6s + 8) = (s+2)(s+4)$：

$$X(s) = \frac{s^2 + 6s + 1}{s(s+2)(s+4)}$$

---

### 4. 部份分式展開 (Partial Fraction Decomposition)

為了能夠順利進行反拉普拉斯轉換，我們必須將 $X(s)$ 拆解為最簡單的部份分式形式：

$$\frac{s^2 + 6s + 1}{s(s+2)(s+4)} = \frac{A}{s} + \frac{B}{s+2} + \frac{C}{s+4}$$

將等號兩邊同乘 $s(s+2)(s+4)$，消除分母：

$$s^2 + 6s + 1 = A(s+2)(s+4) + Bs(s+4) + Cs(s+2)$$

利用**代入法**求出未知的常數 $A, B, C$：

* **令 $s = 0$：**

  $$0^2 + 0 + 1 = A(2)(4) \implies 1 = 8A \implies A = \frac{1}{8}$$

* **令 $s = -2$：**

  $$(-2)^2 + 6(-2) + 1 = B(-2)(-2+4) \implies -7 = -4B \implies B = \frac{7}{4}$$

* **令 $s = -4$：**

  $$(-4)^2 + 6(-4) + 1 = C(-4)(-4+2) \implies -7 = 8C \implies C = -\frac{7}{8}$$

將求得的常數代回原式，得到：

$$X(s) = \frac{1/8}{s} + \frac{7/4}{s+2} - \frac{7/8}{s+4}$$

---

### 5. 取反拉普拉斯轉換 (Inverse Laplace Transform)

最後一步，對 $X(s)$ 進行反拉普拉斯轉換 $\mathcal{L}^{-1}$，將其從 $s$ 頻域轉回 $t$ 時域。
根據基本轉換公式 $\mathcal{L}^{-1}\left[ \frac{1}{s} \right] = 1$ 以及 $\mathcal{L}^{-1}\left[ \frac{1}{s-a} \right] = e^{at}$：

$$x(t) = \mathcal{L}^{-1}[X(s)] = \mathcal{L}^{-1}\left[ \frac{1/8}{s} \right] + \mathcal{L}^{-1}\left[ \frac{7/4}{s+2} \right] - \mathcal{L}^{-1}\left[ \frac{7/8}{s+4} \right]$$

#### **🎉 最終解答：**

$$x(t) = \frac{1}{8} + \frac{7}{4}e^{-2t} - \frac{7}{8}e^{-4t}$$
# 題目(8)：求函數 $f(x) = x^2$ 在區間 $[-\pi, \pi]$ 的傅立葉級數
*(出自：成大考研工程數學)*

---

### 1. 判斷函數的奇偶性與設定級數公式

已知函數為 $f(x) = x^2$，區間為 $x \in [-\pi, \pi]$。
因為 $f(-x) = (-x)^2 = x^2 = f(x)$，所以 $f(x)$ 是一個**偶函數 (Even Function)**。

對於週期為 $2\pi$ 的偶函數，其傅立葉級數只會包含餘弦項（Cosine terms）與常數項，正弦項（Sine terms）的係數 $b_n$ 必定為 $0$。

傅立葉級數的標準形式可簡化為：
$$f(x) = \frac{a_0}{2} + \sum_{n=1}^{\infty} a_n \cos(nx)$$

---

### 2. 計算常數項係數 $a_0$

根據傅立葉係數公式，並利用偶函數在對稱區間積分的特性（可改為 $0$ 到 $\pi$ 積分的兩倍）：

$$a_0 = \frac{1}{\pi} \int_{-\pi}^{\pi} x^2 \, dx = \frac{2}{\pi} \int_{0}^{\pi} x^2 \, dx$$

進行基本的定積分計算：

$$a_0 = \frac{2}{\pi} \left[ \frac{x^3}{3} \right]_{0}^{\pi} = \frac{2}{\pi} \left( \frac{\pi^3}{3} - 0 \right) = \frac{2\pi^2}{3}$$

所以，級數首項常數為 $\frac{a_0}{2} = \frac{\pi^2}{3}$。

---

### 3. 計算餘弦項係數 $a_n$

同樣利用偶函數特性：

$$a_n = \frac{1}{\pi} \int_{-\pi}^{\pi} x^2 \cos(nx) \, dx = \frac{2}{\pi} \int_{0}^{\pi} x^2 \cos(nx) \, dx$$

這裡需要使用**分部積分法 (Integration by Parts)** 兩次。令 $u = x^2$ 且 $dv = \cos(nx)dx$：

$$\int x^2 \cos(nx) \, dx = x^2 \left( \frac{\sin(nx)}{n} \right) - \int 2x \left( \frac{\sin(nx)}{n} \right) \, dx$$
$$= \frac{x^2 \sin(nx)}{n} - \frac{2}{n} \left[ x \left( -\frac{\cos(nx)}{n} \right) - \int 1 \cdot \left( -\frac{\cos(nx)}{n} \right) \, dx \right]$$
$$= \frac{x^2 \sin(nx)}{n} + \frac{2x \cos(nx)}{n^2} - \frac{2 \sin(nx)}{n^3}$$

現在將上下限 $0$ 與 $\pi$ 代入：

$$\int_{0}^{\pi} x^2 \cos(nx) \, dx = \left[ \frac{x^2 \sin(nx)}{n} + \frac{2x \cos(nx)}{n^2} - \frac{2 \sin(nx)}{n^3} \right]_{0}^{\pi}$$

* **代入上限 $\pi$**：$\sin(n\pi) = 0$ 且 $\cos(n\pi) = (-1)^n$。
  得出：$0 + \frac{2\pi(-1)^n}{n^2} - 0$
* **代入下限 $0$**：$\sin(0) = 0$ 且 $\cos(0) = 1$。
  得出：$0 + 0 - 0 = 0$

將計算結果代回 $a_n$ 的公式：

$$a_n = \frac{2}{\pi} \left( \frac{2\pi(-1)^n}{n^2} \right) = \frac{4(-1)^n}{n^2}$$

---

### 4. 確認正弦項係數 $b_n$

因為 $f(x) = x^2$ 為偶函數，且 $\sin(nx)$ 為奇函數，兩者相乘為奇函數。奇函數在對稱區間 $[-\pi, \pi]$ 的積分為 $0$：

$$b_n = \frac{1}{\pi} \int_{-\pi}^{\pi} x^2 \sin(nx) \, dx = 0$$

---

### 5. 最終解答

將求得的 $\frac{a_0}{2}$ 與 $a_n$ 代回傅立葉級數展開式中，得到最終答案：

$$f(x) = \frac{\pi^2}{3} + \sum_{n=1}^{\infty} \frac{4(-1)^n}{n^2} \cos(nx)$$

展開前幾項可以寫成：
$$x^2 = \frac{\pi^2}{3} - 4\cos(x) + \cos(2x) - \frac{4}{9}\cos(3x) + \dots$$
